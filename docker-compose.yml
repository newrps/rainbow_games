# 정적 HTML 서빙 (nginx) — NAS 18084 → 80
name: board-games

services:
  web:
    image: nginx:alpine
    container_name: board-games
    restart: unless-stopped
    volumes:
      - ./:/usr/share/nginx/html:ro
    ports:
      - "18084:80"
